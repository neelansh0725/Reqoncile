# Reqoncile — top-level dependencies (TechStack §7)
# Pinned to versions verified against Python 3.14 on arm64 macOS.
# Full transitive lock lives in requirements.lock.txt (regenerate with:
#   .venv/bin/pip freeze > requirements.lock.txt)

# --- Orchestration ---
langchain==1.3.15
langchain-community==0.4.2

# --- LLM providers (two tiers; see config.py / .env.example) ---
langchain-google-genai==4.3.3     # reasoning tier: Gemini free tier
langchain-ollama==1.1.0           # generation tier: local Ollama
langchain-anthropic==1.5.6        # optional third provider, kept swappable
anthropic==0.122.0

# --- Retrieval ---
chromadb==1.5.9
sentence-transformers==5.7.0

# --- Lexical retrieval (T026a) ---
snowballstemmer==3.0.1            # Porter2 stemming for BM25

# --- Document ingestion ---
pypdf==6.16.1

# --- Backend ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.20          # multipart/form-data for PDF upload

# --- Schemas & config ---
pydantic==2.13.4
python-dotenv==1.2.2

# --- Testing ---
pytest==9.1.0
